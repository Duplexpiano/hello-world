#include <stdio.h>

int main()
{
    int i, min = 0, n;
    int a[10];
    scanf("%d", &n);
    for(i = 0; i < n; i++)
        scanf("%d", &a[i]);
    for(i = 1; i < n; i++)
        if(a[i] < a[min])   min = i;
    printf("%d %d\n", a[min], min);
    return 0;
}
