#include <stdio.h>

int main()
{
    int i, flag = 0, x, n;
    int a[50];
    scanf("%d %d", &n, &x);
    for(i = 0; i < n; i++)
        scanf("%d", &a[i]);
    for(i = 0; i < n; i++)
        if(a[i] == x)
        {
            printf("%d\n", i);
            flag = 1;
            break;
        }
    if(flag == 0)
        printf("Not Found\n");
    return 0;
}
